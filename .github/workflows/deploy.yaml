name: Test Workflow

on : 
    push:
        branches:
            - main
        
jobs:
    test:
        runs-on: ubutnu:latest

        steps:
            - name : Print Message
              run : echo "hello Priyansh Github Action Working!"